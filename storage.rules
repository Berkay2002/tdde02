rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TODO: tighten rules for production
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
